package controls;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class DropDown extends ObjectControl {

    public DropDown(WebDriver webDriver, By by) {
        super(webDriver, by);
    }

}
