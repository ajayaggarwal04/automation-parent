package controls;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class CheckBox extends ObjectControl {

    public CheckBox(WebDriver webDriver, By by) {
        super(webDriver, by);
    }

}
