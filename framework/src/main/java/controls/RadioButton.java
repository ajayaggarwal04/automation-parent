package controls;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class RadioButton extends ObjectControl {

    public RadioButton(WebDriver webDriver, By by) {
        super(webDriver, by);
    }

}
