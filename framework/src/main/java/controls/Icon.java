package controls;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Icon extends ObjectControl {

    public Icon(WebDriver webDriver, By by) {
        super(webDriver, by);
    }

}
