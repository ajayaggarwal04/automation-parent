package controls;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class WebObject extends ObjectControl {

    public WebObject(WebDriver webDriver, By by) {
        super(webDriver, by);
    }

}
